#In the specified indoor.py file, you're tasked to create a Python program. This program should prompt the user for input, and then output the same input but in lowercase, leaving punctuation and whitespace unchanged. You may, but are not required to, provide a custom prompt to the user using a string argument with the input function.

input_words = input("")
lower_input_words = input_words.lower()
print(lower_input_words)